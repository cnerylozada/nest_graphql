# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Pet {
  id: ID!
  name: String!
  type: PetType!
}

enum PetType {
  CAT
  DOG
  BIRD
}

type Author {
  id: ID!
  firstName: String!
  books: [Book!]!
}

type Book {
  id: ID!
  title: String!
  votes: Int
}

type User {
  id: Int!
  fullName: String!
  bio: String!
  image: String!
  email: String!
  posts: [Post!]
}

type Post {
  id: Int!
  text: String!
  video: String!
}

type Query {
  pets: [Pet!]!
  getAllAuthors: [Author!]!
  getAuthorById(id: ID!): Author!
  getAllUsers: [User!]!
}

type Mutation {
  savePet(pet: CreatePetInputDto!): Pet!
  deletePetById(petId: ID!): ID!
  saveAuthor(author: CreateAuthorInputDto!): Author!
  saveBookInAuthor(book: CreateBookInputDto!, authordId: ID!): Book!
}

input CreatePetInputDto {
  name: String!
  type: PetType!
}

input CreateAuthorInputDto {
  firstName: String!
}

input CreateBookInputDto {
  title: String!
}